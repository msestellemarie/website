version: 0.2
phases:
  build:
    commands:
      - npm install -g yarn
      - sudo apt install hugo -y
      - yarn
      - echo Starting build
      - hugo --config hugo.toml
      - yarn run build
      - echo Build finished
artifacts:
  name: "BuildOutput"
  files:
    - "**/*"
  base-directory: public
    